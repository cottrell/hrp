from setuptools import setup

setup(name='hrp',
      version='0.0',
      description='HRP copy/paste fix and refactor.',
      packages=['hrp'],
      install_requires=['numpy', 'scipy', 'pandas'],
      zip_safe=False)
